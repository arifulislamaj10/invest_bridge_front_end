# # Use /api proxy (recommended) — works on localhost and 127.0.0.1
# NEXT_PUBLIC_API_URL=/api
# API_PROXY_URL=http://localhost:5000

# Or call backend directly (requires CORS match):
# NEXT_PUBLIC_API_URL=http://localhost:5000/api
